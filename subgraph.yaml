specVersion: 0.0.1
schema:
  file: ./schema.graphql
dataSources:
  - kind: near
    name: contract_v2
    network: near-testnet
    source:
      account: "identicon.testnet"
    mapping:
      apiVersion: 0.0.5
      language: wasm/assemblyscript
      entities:
        - ExampleEntity
      receiptHandlers:
        - handler: handleReceipt
      file: ./src/mapping.ts
